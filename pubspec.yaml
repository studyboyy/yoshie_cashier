name: flutter_cashier
description: "Yosy Group — Aplikasi kasir Android."
publish_to: 'none'
version: 1.1.17+19
environment:
  sdk: ^3.12.2

dependencies:
  connectivity_plus: ^7.1.1
  flutter:
    sdk: flutter
  http: ^1.6.0
  shared_preferences: 2.5.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/brand.png
